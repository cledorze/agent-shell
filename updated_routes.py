# Import existing routes code and replace VM manager import with:
from robust_vm_manager import RobustVMManager

# Then initialize the VM manager:
vm_manager = RobustVMManager()

# The rest of your routes code remains the same
